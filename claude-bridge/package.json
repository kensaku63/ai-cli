{
  "name": "ai-cli-bridge",
  "version": "0.1.0",
  "description": "MCP server bridge — Claude Code connects to ai-cli as a single tool router",
  "type": "module",
  "bin": {
    "ai-cli-bridge": "./dist/claude-bridge/src/index.js"
  },
  "scripts": {
    "build": "tsc && cp -r ../tool-registry/data dist/tool-registry/",
    "start": "node dist/claude-bridge/src/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
